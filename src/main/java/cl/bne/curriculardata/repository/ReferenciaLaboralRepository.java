package cl.bne.curriculardata.repository;

import cl.bne.curriculardata.domain.ReferenciaLaboral;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReferenciaLaboralRepository extends JpaRepository<ReferenciaLaboral, Long> {
}